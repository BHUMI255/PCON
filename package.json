{
  "name": "pcon-unified-deployment",
  "version": "1.0.0",
  "dependencies": {
    "@prisma/client": "^5.12.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.12.1"
  },
  "scripts": {
    "postinstall": "cd backend && npx prisma generate",
    "build": "cd frontend && npm install && npm run build"
  }
}
